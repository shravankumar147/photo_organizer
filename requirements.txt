# photo-organizer — Python 3.10+
# ─────────────────────────────────────────────────────────────────────────────
# Core (image metadata)
Pillow>=10.0.0
omegaconf>=2.3.0

# Dev / testing
pytest>=8.0.0
pytest-cov>=5.0.0

# ─────────────────────────────────────────────────────────────────────────────
# Optional — uncomment when you need them:
#
# tqdm>=4.66.0          # progress bars
# pyyaml>=6.0           # YAML config support
# fastapi>=0.111.0      # REST API wrapper
# uvicorn>=0.29.0       # ASGI server for FastAPI
# openai-clip           # CLIP embeddings (AI phase)
# torch>=2.0.0          # required by CLIP
